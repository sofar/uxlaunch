#!/bin/sh

echo "no configure needed, this is just a test:"

echo -n "Checking for dbus-1... " &&
pkg-config --modversion dbus-1 &&
echo -n "Checking for ck-connector... " &&
pkg-config --modversion ck-connector &&
echo -n "Checking for glib-2.0... " &&
pkg-config --modversion glib-2.0
